<%- include('partials/header', { pageTitle: t('booking.successTitle') }) %>

  <section class="section">
    <div class="container">
      <div class="success-card">
        <div class="success-icon">✓</div>
        <h2><%= t('booking.successTitle') %></h2>
        <p class="muted"><%= t('booking.successBody') %></p>
        <p style="margin-top:20px;"><a href="/" class="btn btn-secondary"><%= t('booking.backHome') %></a></p>
      </div>
    </div>
  </section>

<%- include('partials/footer') %>
